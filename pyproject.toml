[project]
name = "tts-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aiosqlite>=0.22.1",
    "discord-py[voice]>=2.7.1",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.14.2",
    "tts-client",
]

[tool.uv.sources]
tts-client = { git = "https://github.com/haruyq/tts-client.git" }
